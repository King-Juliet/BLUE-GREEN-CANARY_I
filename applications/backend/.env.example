PORT=3000
NODE_ENV=development
AWS_REGION=us-east-1
DB_CREDENTIALS_SSM_NAME=/bluegreen-canary/db/credentials
# Optional local fallback only for local developer testing.
# Do not hard-code production credentials in source control.
DATABASE_URL=postgresql://appuser@localhost:5432/appdb
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=
DB_SSL=false
